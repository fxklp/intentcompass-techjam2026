# The base local/fallback path uses only the Python standard library.
# Python 3.12 or 3.13 with SQLite FTS5 is required.
# For the full on-demand dense and cross-encoder path, also install
# requirements-semantic.txt and run scripts/build_semantic_index.py --download.
